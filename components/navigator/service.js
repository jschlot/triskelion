angular
    .module('triskelion.navigator.service', [])
    .factory('navigator', [
        function() {
            'use strict';
            var navigator = {
            };
            return navigator;
        }
    ]);